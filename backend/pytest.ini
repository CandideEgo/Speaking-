[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
markers =
    unit: Unit tests (fast, no external dependencies)
    integration: Integration tests (requires database)
addopts = -v --tb=short
