# Handoff: 2026-09-20 baseline

- Owner: all
- Status: done
- 用途：基线条目 + 格式实例。此条目登记多 Agent 协议落地时的工作区状态。

## 任务

记录协议启用基线：确认无跨 Owner 未交接的契约变更，后续拆分任务从此接续。

## 已完成

- 工作区干净（2026-09-20 核实），无未提交改动。
- 此前挂起的部署配置与 `video_cache.py` / `vocab_set_service.py` 修复已于 2026-09-19
  至 2026-09-20 提交落地（提交主题：`chore(deploy): 内测部署配置与物料`、
  `fix(backend): 排行快照缓存失效 + 并发收词唯一约束竞态`）。
- 多 Agent 协议三件套落地：`.agent/owners.md`、`handoffs/README.md`、本条目。

## 契约变更

无（本条目不改动代码）。

## 关键决策

- 实例：DEC-038（feed 卡片信息与排行榜日历周）是一个标准的跨 Owner 变更，
  其交接要点应记录为——
  - `schemas/video.py`：`VideoResponse` 新增 `view_count`（站内完整播放事件，
    区别于 `ext_view_count`）与 `description`；列表序列化截断至
    `CARD_DESCRIPTION_LIMIT`（200 字符），详情页保留全文。
  - 前端两端同步：`types/index.ts` 与 `types/platform.ts` 均已加字段；
    `VideoCard` 渲染两行简介与 Eye/Bookmark 指标行；favorites 页透传
    `storage_mode` 以显示离线徽标。
  - 该实例展示了契约变更双向登记的最低信息量：字段语义 + 截断规则 + 各端落点。

## 遗留

- 部署文件（`docker-compose.prod.yml` / `nginx.ssl.conf` / `deploy*.sh`）已提交，
  但其明文服务器信息是否已脱敏仍待用户确认——不在本协议范围，见 `state.md`。
