开发启动：`npm run dev`（Next dev 服务默认 3000 端口，通过 next.config.js 的 rewrites 将 `/api/` 和 `/media/` 代理到后端 localhost:8000）。E2E 测试：`npm run test:e2e`，Playwright 会自动启动后端 uvicorn 和前端 dev server。构建产物为 standalone 输出模式。环境变量通过 `.env.example` 配置，主要依赖 `NEXT_PUBLIC_API_URL`。
