开发启动：`docker compose -f docker-compose.dev.yml up -d` 启动基础设施，再分别运行 backend、celery、GPU worker 与 frontend；Windows 提供 `/speaking-dev` 一键脚本。生产部署：`docker compose -f docker-compose.prod.yml up -d` 由 Nginx 统一入口。测试：backend 用 `pytest tests/`，frontend 用 `npx tsc --noEmit && npm run check`。
