node_modules/
.next/
__pycache__/
*.pyc
# IDE planning artifacts (local agent session outputs)
.qoder/
# Environment files (whitelist: only *.env.example may be committed)
*.env*
!*.env.example
*.egg-info/
dist/
build/
.vercel/
media/
venv/
*.tsbuildinfo
.venv/
wsl.localhost/
.ruff_cache/
.pytest_cache/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Windows NUL redirect artifacts
nul
NUL

# Logs
logs/*.log
*.log
# 运维脚本与产物：logs/ 下除 .gitkeep 外一律不入库。
# 原因：这里会落 admin JWT（logs/admin_token.txt）、生产服务器访问脚本等
# 本地运维物料 —— 曾有一次 admin_token.txt 出现在未跟踪列表里（从未提交过，
# 但只差一次 `git add -A`）。真需要版本化的运维脚本放 backend/scripts/ 或
# scripts/。见 wiki/problems/cache-invalidation-and-media-gate-blindspots.md 同期笔记。
logs/*
!logs/.gitkeep

# 一次性迁移/抓取临时产物（非可复用代码）
.deploy-tmp/
.lr-scrape/
backend/scripts/_*.txt
.claude/plans/

# Temporary files
backend/temp/
backend/tmp/
backend/transcripts/
backend/reprocess_export/

# ECDICT word database (downloaded via scripts/download_ecdict.py, ~30MB)
backend/data/

# Cookie backups
backend/youtube_cookies*.txt
backend/youtube_state.json

# Screenshots
*_test.png
homepage*.png
watch_page.png

# Experimental scripts (kept in git history, ignored for future)
backend/batch_transcribe.py
backend/retranscribe*.py
backend/translate_subtitles.py
backend/seed_test_video.py
backend/seed_whisper_video.py
export_cookies.js

# Large media files
test_audio.wav
*.wav

# Playwright CLI artifacts
.playwright/
.playwright-cli/
backend/.playwright-cli/
frontend/.playwright-cli/

# Playwright test output (e2e run artifacts)
frontend/test-results/
frontend/playwright-report/

# Backend build artifacts (should never exist)
backend/.next/
backend/.mypy_cache/

# Backend nested directory (accident)
backend/backend/

# Temporary test/debug files
tmp_audio*
test_whisperx_llm.py
whisper-sentence-segmentation-*.md

# Local agent / workflow artifacts
.gsd/
.qoder/
.claude/worktrees/
.claude/model_audit_output.json
.claude/loop-state.local.md
.claude/scheduled_tasks.lock

# Orphaned cookie-acquisition script (youtube platform module removed)
frontend/yt_cookies.mjs

# Debug screenshots (generated during dev)
.debug-shots/
frontend/.debug-shots/
backend/watch-current.png

# Project planning notes (not code) — moved to docs/plans/
# 后续开发项目.txt
